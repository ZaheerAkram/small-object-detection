review the full code update the notebook to the local harware machine training, only set the epoch to 150 static no need for the calculating it. moreover add the early stopping in the code for 15. Everything should get save iteratively. Make sure the code should be capable of auto training from the last best weights it saved if aviable aevery time if not then train again, currently when i train on my machine and then run the model for 2 epochs and i interupted my training .ipynb file inbetween and run again that cell it starts from the begining epoch instead of where it left. make sure to also make this notebook. gpu and ram friendly as i have 15 gb ram and 8 gb gpu. optimize the code that my ram should not go out of memory, try to utilize the ram effeciently because, almost 7,8gb is already get full due to other services in computer.

also create the requirements.txt file without version in the packages.